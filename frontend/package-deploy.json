{
  "name": "isro-avalanche-subnet-deployment",
  "version": "1.0.0",
  "description": "Deployment scripts for ISRO Avalanche Subnet",
  "main": "deploy-avalanche-subnet.js",
  "scripts": {
    "deploy": "node deploy-avalanche-subnet.js",
    "deploy-subnet": "bash deploy-subnet-avalanche-cli.sh",
    "generate-accounts": "node -e \"const { generateAccounts } = require('./deploy-avalanche-subnet.js'); generateAccounts();\"",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "ethers": "^6.8.1"
  },
  "devDependencies": {
    "jq": "^1.6.0"
  },
  "keywords": [
    "avalanche",
    "subnet",
    "isro",
    "blockchain",
    "deployment"
  ],
  "author": "ISRO Development Team",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
